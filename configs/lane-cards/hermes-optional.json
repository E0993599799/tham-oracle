{
  "lane_id": "hermes-optional",
  "agent": "hermes",
  "soul": "Hermes — Gemini Specialist Adapter",
  "description": "Hermes specialist/legacy adapter — ใช้เฉพาะเมื่อ Tham explicitly route มาเท่านั้น ห้ามใช้ default",
  "input_format": "structured JSON task contract",
  "output_format": "completion result + proof",
  "provider": "gemini",
  "base_url": "http://127.0.0.1:20128/v1",
  "model": "gemini/gemini-2.5-flash",
  "api_key_env": "OPENCLAW_API_KEY",
  "gemini_key_note": "Gemini API key is managed by 9router internally — no separate env needed in caller",
  "routes_through": "9router/OpenClaw (port 20128)",
  "risk_gate": "inherit from Tham",
  "memory_gate": "read-only",
  "proof_required": true,
  "when_to_use": [
    "Tasks explicitly routed by Executor Lane Router to Hermes",
    "Specialist tasks where gemini-2.5-flash has demonstrated advantage",
    "Multimodal or long-context tasks suited for Gemini"
  ],
  "when_NOT_to_use": [
    "Default fallback when Claude is slow — never",
    "Tasks Claude Sonnet/Haiku can handle",
    "Shortcutting around main execution lanes",
    "Any task without explicit route decision"
  ],
  "invocation_contract": {
    "lane": "hermes-optional",
    "task": "...",
    "reason_for_hermes": "...",
    "structured_input": {},
    "expected_output": "...",
    "proof_required": true
  },
  "health_check": "curl http://127.0.0.1:20128/v1/models | jq '.data[] | select(.id | contains(\"gemini\"))'",
  "rules": [
    "NEVER route to Hermes by default",
    "Always use structured JSON contract — no raw natural language",
    "Verify 9router is running on port 20128 before invoke",
    "Verify GEMINI_API_KEY is set in 9router environment",
    "Watch for shim recursion — Hermes must not call itself"
  ],
  "status": "active",
  "verified": "2026-05-16"
}
