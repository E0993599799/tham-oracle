# Secrets
.env
.env.*
*.key
*.pem
*.p12
*.pfx
*token*
!skills/token-optimizer/
*secret*
!skills/security-secret-hygiene/
*credential*

# OS
.DS_Store
Thumbs.db

# Dependencies
node_modules/

# Logs / temporary
*.log
tmp/
temp/
.cache/

# Oracle vault — persistent (tracked) vs. ephemeral (excluded)
ψ/inbox/          # Ephemeral: handoffs, escalations, inbox notes (14d expiry)
ψ/active/         # Sprint-scoped: active goals, blockers, temporary focus
ψ/**/data/        # Transient session data (logs, caches, temp)
ψ/learn/**/origin # Symlink origin markers (session-local)
!/ψ/memory/       # INCLUDE: persistent learnings, patterns, decisions (tracked)
!/ψ/memory/**
!/ψ/fleet/        # INCLUDE: fleet-wide strategy, config (tracked)
!/ψ/fleet/**
!/ψ/resonance/    # INCLUDE: joy moments, connection logs (tracked)

# maw config (contains federationToken — never commit)
~/.config/maw/maw.config.json

# Local setup logs (commit summary files manually if needed)
.oracle-setup/logs/

# Supabase local temp state
supabase/.temp/

# Tools runtime logs (empty dirs only — logs excluded by *.log rule)
tools/logs/

# Added by Multi-Agent Workflow Kit
/.agents
/.envrc
/MAW-*
.claude/settings.local.json
.claude/*
!.claude/commands/
.codex/*
!.codex/prompts/
!.codex/prompts/**
.claude/commands/maw.*
.claude/commands/maw-*
.codex/prompts/maw*.md
dashboard/data.json
.telegram-config
scripts/__pycache__/

# Auto-save transient lock file
brain/memory/.auto-save.lock

# Fleet status broadcast utilities (tracked)
!/scripts/fleet-*.sh
