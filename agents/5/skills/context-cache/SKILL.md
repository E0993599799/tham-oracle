# Skill: Context Cache Engine

## Purpose
Keep recurring project facts available without repeatedly reloading everything.

## Rules
- Cache baseline, ports, paths, agents, routes, policies, and latest proof.
- Invalidate cache when live proof contradicts memory.
- Prefer compact current-state snapshots.

