export function initializeAutoRunRuntime() {
  // Dashboard build-time instrumentation hook.
  // The dashboard app does not need the Forge V2 auto-run runtime here,
  // but Next.js instrumentation expects the module to exist.
}
