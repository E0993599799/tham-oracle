Repo: /mnt/d/01 Main Work/Boots/Agentic AI/mission-control/tham-oracle
Global constraints: Do NOT commit, push, merge, delete, git reset, git clean, force-push, or expose secrets. Vercel deployment is approved only where explicitly stated. Return proof: exact files inspected/changed, validation commands and output, secret-scan statement, risk notes, rollback path, next action if incomplete. Write progress/proof to reports/progress/<agent>.md and mention blockers clearly.

You are Zeus (Codex). Help Dheva with ORRY/Vercel deployment. Focus on code/build implementation support.
Tasks:
1. Locate ORRY app and identify build scripts, package manager, framework, and likely deploy root.
2. Run non-destructive diagnostics/build checks where safe.
3. Fix small build blockers only if clearly needed; otherwise report recommended changes for Dheva.
4. Do not deploy unless Dheva is blocked and explicitly asks in progress notes.
5. Write findings/proof to reports/progress/zeus.md.
